<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IA & Création d'Images | Projet 2S1</title>
    <style>
        /* GLOBAL STYLES & VARIABLES */
        :root {
            --bg-dark: #0f172a;
            --card-bg: rgba(30, 41, 59, 0.7);
            --primary: #3b82f6; /* Neon Blue */
            --accent: #8b5cf6;  /* Purple */
            --text-main: #f8fafc;
            --text-muted: #94a3b8;
            --gradient: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Inter', system-ui, -apple-system, sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-main);
            line-height: 1.8;
            overflow-x: hidden;
        }

        /* Animated Background */
        .background-glow {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            background: radial-gradient(circle at 50% 50%, rgba(59, 130, 246, 0.15), transparent 70%);
            z-index: -1;
            pointer-events: none;
        }

        /* HEADER / HERO SECTION */
        header {
            min-height: 90vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 2rem;
            position: relative;
        }

        h1 {
            font-size: 3.5rem; /* Default for large screens */
            font-weight: 800;
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 1rem;
            line-height: 1.2;
            animation: fadeInDown 1s ease-out;
        }

        .subtitle {
            font-size: 1.5rem;
            color: var(--text-muted);
            margin-bottom: 3rem;
            font-weight: 300;
            animation: fadeInUp 1s ease-out 0.3s backwards;
        }

        .meta-info {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
            justify-content: center;
            animation: fadeInUp 1s ease-out 0.6s backwards;
        }

        .badge {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            padding: 0.5rem 1.5rem;
            border-radius: 50px;
            font-size: 0.9rem;
            backdrop-filter: blur(10px);
        }

        .students {
            margin-top: 2rem;
            font-size: 1.2rem;
            font-weight: bold;
            color: var(--primary);
            animation: fadeInUp 1s ease-out 0.9s backwards;
        }

        /* CONTAINER & CARDS */
        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 4rem 1.5rem;
        }

        section {
            margin-bottom: 6rem;
            opacity: 0; 
            transform: translateY(30px);
            transition: all 0.8s ease-out;
        }

        section.visible {
            opacity: 1;
            transform: translateY(0);
        }

        h2 {
            font-size: 2.5rem;
            margin-bottom: 2rem;
            border-left: 5px solid var(--accent);
            padding-left: 1rem;
            display: inline-block;
        }

        .card {
            background: var(--card-bg);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 2.5rem;
            backdrop-filter: blur(12px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            border-color: var(--primary);
        }

        /* TIMELINE & STEPS STYLES */
        .step-list {
            list-style: none;
        }

        .step-item {
            display: flex;
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .step-number {
            font-size: 2rem;
            font-weight: 900;
            color: rgba(255, 255, 255, 0.1);
            min-width: 50px;
        }

        .step-content h3 {
            color: var(--primary);
            margin-bottom: 0.5rem;
        }

        /* CODE / LANGUAGES STYLE */
        .tech-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .tech-box {
            background: rgba(255,255,255,0.05);
            padding: 1.5rem;
            border-radius: 12px;
            text-align: center;
            border: 1px solid transparent;
            transition: all 0.3s;
        }

        .tech-box:hover {
            border-color: var(--accent);
            background: rgba(139, 92, 246, 0.1);
        }

        .tech-name {
            font-weight: bold;
            font-size: 1.2rem;
            display: block;
            margin-bottom: 0.5rem;
            color: var(--accent);
        }

        /* SUMMARY BOX */
        .summary-box {
            background: linear-gradient(135deg, rgba(59, 130, 246, 0.2), rgba(139, 92, 246, 0.2));
            padding: 2rem;
            border-radius: 15px;
            text-align: center;
            margin-top: 2rem;
            border: 1px solid rgba(255,255,255,0.1);
            font-size: 1.1rem;
            font-weight: 500;
        }

        footer {
            text-align: center;
            padding: 3rem;
            border-top: 1px solid rgba(255,255,255,0.1);
            color: var(--text-muted);
            margin-top: 4rem;
        }

        /* ======================================= */
        /* MEDIA QUERIES - التجاوب مع شاشات الهاتف */
        /* ======================================= */
        @media (max-width: 600px) {
            
            header {
                min-height: 50vh;
                padding: 1rem;
            }

            h1 {
                font-size: 2.2rem; /* تصغير العنوان على الهاتف */
                margin-top: 1rem;
            }

            .subtitle {
                font-size: 1.1rem;
                margin-bottom: 2rem;
            }

            .container {
                padding: 2rem 1rem;
            }

            section {
                margin-bottom: 4rem;
            }

            h2 {
                font-size: 2rem;
            }

            .card {
                padding: 1.5rem;
            }

            .step-item {
                flex-direction: column; /* ترتيب الخطوات عمودياً */
                gap: 0.5rem;
            }

            .step-number {
                min-width: unset;
                align-self: flex-start;
            }

            .students {
                font-size: 1rem;
            }
        }

        /* ANIMATIONS KEYFRAMES */
        @keyframes fadeInDown {
            from { opacity: 0; transform: translateY(-30px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

    </style>
</head>
<body>

    <div class="background-glow"></div>

    <header>
        <h1>Création d’images<br>avec l’Intelligence Artificielle</h1>
        <div class="subtitle">Étude d’un outil d’IA générateur d’images</div>
        
        <div class="meta-info">
            <span class="badge">Matière : Français</span>
            <span class="badge">Classe : 2S1</span>
            <span class="badge">Année : 2025/2026</span>
        </div>

        <div class="students">
            Amamra Anis &nbsp; • &nbsp; Benguettat Abdellah
        </div>
    </header>

    <div class="container">

        <section>
            <h2>Présentation de ChatGPT</h2>
            <div class="card">
                <p><strong>ChatGPT</strong> est un modèle avancé de traitement du langage naturel développé par la société américaine <strong>OpenAI</strong>. Il est conçu pour comprendre des textes, répondre à des questions, assister dans la rédaction, traduire des langues et générer des contenus de manière cohérente et naturelle.</p>
                <br>
                <p>La première version publique de ChatGPT a été lancée en <strong>novembre 2022</strong>. Depuis, plusieurs versions améliorées ont vu le jour, offrant une compréhension plus précise، une capacité de raisonnement plus avancée، et une interaction plus fluide avec les utilisateurs. Grâce à ses performances, ChatGPT est aujourd’hui largement utilisé dans l’éducation, la recherche, la programmation, la communication et la création de contenu.</p>
            </div>
        </section>

        <section>
            <h2>Comment fonctionne ChatGPT ?</h2>
            <div class="card">
                <p style="margin-bottom: 2rem;">ChatGPT fonctionne grâce à une technologie appelée <strong>modèle de langage</strong>. Il s’agit d’un système d’IA capable de comprendre et de générer du texte de manière naturelle.</p>
                
                <div class="step-list">
                    <div class="step-item">
                        <div class="step-number">01</div>
                        <div class="step-content">
                            <h3>Collecte des données</h3>
                            <p>Le modèle est entraîné sur une énorme quantité de textes provenant de livres, d’articles, de sites web et de documents éducatifs. Cette étape permet à l’IA de comprendre la structure de la langue et d’apprendre le vocabulaire.</p>
                        </div>
                    </div>

                    <div class="step-item">
                        <div class="step-number">02</div>
                        <div class="step-content">
                            <h3>Entraînement du modèle</h3>
                            <p>ChatGPT utilise une architecture avancée appelée <strong>Transformers</strong>. Grâce à cette technologie, le modèle apprend à analyser les phrases، comprendre le contexte، prédire les mots suivants et produire des réponses logiques.</p>
                        </div>
                    </div>

                    <div class="step-item">
                        <div class="step-number">03</div>
                        <div class="step-content">
                            <h3>Compréhension de la question</h3>
                            <p>Quand l’utilisateur écrit un message، ChatGPT lit la phrase، identifie les mots importants، analyse l’intention et comprend le sens général de la demande.</p>
                        </div>
                    </div>

                    <div class="step-item">
                        <div class="step-number">04</div>
                        <div class="step-content">
                            <h3>Génération de la réponse</h3>
                            <p>Après l’analyse، l’IA génère une réponse en choisissant les mots les plus appropriés، en construisant des phrases correctes et en respectant le contexte. La réponse est produite en quelques millisecondes.</p>
                        </div>
                    </div>
                </div>

                <div class="summary-box">
                    📝 <strong>Résumé :</strong><br>
                    ChatGPT lit la question → la comprend → utilise ses connaissances → et génère une réponse claire et naturelle.
                </div>
            </div>
        </section>

        <section>
            <h2>Langages utilisés</h2>
            <div class="card">
                <p>ChatGPT est principalement développé avec le langage de programmation <strong>Python</strong>, utilisé grâce à ses bibliothèques puissantes :</p>
                
                <div class="tech-grid">
                    <div class="tech-box">
                        <span class="tech-name">TensorFlow</span>
                        Bibliothèque Google
                    </div>
                    <div class="tech-box">
                        <span class="tech-name">PyTorch</span>
                        Machine Learning
                    </div>
                    <div class="tech-box">
                        <span class="tech-name">NumPy</span>
                        Calcul mathématique
                    </div>
                    <div class="tech-box">
                        <span class="tech-name">Transformers</span>
                        Hugging Face
                    </div>
                </div>

                <p style="margin-top: 2rem;">D’autres langages sont également utilisés dans le fonctionnement global :</p>
                <ul style="margin-left: 20px; margin-top: 10px; color: var(--text-muted);">
                    <li><strong>C++ :</strong> pour optimiser les performances et accélérer les calculs.</li>
                    <li><strong>CUDA :</strong> pour permettre aux cartes graphiques (GPU) d’effectuer les calculs.</li>
                    <li><strong>JavaScript :</strong> pour les interfaces web qui utilisent ChatGPT.</li>
                </ul>
            </div>
        </section>

        <section>
            <h2>Génération d'images (IA)</h2>
            <div class="card">
                <p>Les outils comme DALL-E, Midjourney ou Stable Diffusion transforment une description écrite en image. Voici les étapes :</p>
                
                <div style="margin-top: 2rem;">
                    
                    <div class="step-item">
                        <div class="step-number">1</div>
                        <div class="step-content">
                            <h3>Compréhension du texte</h3>
                            <p>Pour « un chat robot dans l’espace », l'IA analyse les mots, identifie les objets, comprend le style et interprète la relation entre les éléments.</p>
                        </div>
                    </div>

                    <div class="step-item">
                        <div class="step-number">2</div>
                        <div class="step-content">
                            <h3>Encodage du texte</h3>
                            <p>Le texte est transformé en représentations numériques appelées <strong>embeddings</strong>. Cela convertit les mots en concepts visuels internes.</p>
                        </div>
                    </div>

                    <div class="step-item">
                        <div class="step-number">3</div>
                        <div class="step-content">
                            <h3>Génération (Diffusion Model)</h3>
                            <p>Le modèle commence par une image totalement bruitée (noise). Étape par étape، il retire ce bruit et ajoute des formes، des couleurs et des détails. C’est comme si l’image se dessinait petit à petit. </p>
                        </div>
                    </div>

                    <div class="step-item">
                        <div class="step-number">4</div>
                        <div class="step-content">
                            <h3>Amélioration des détails</h3>
                            <p>Quand la forme générale est créée، l’IA améliore les textures، ajuste les lumières، et ajoute les détails fins.</p>
                        </div>
                    </div>

                    <div class="step-item">
                        <div class="step-number">5</div>
                        <div class="step-content">
                            <h3>Résultat final</h3>
                            <p>À la fin، l’IA produit une image complète et originale qui correspond au texte initial. Elle peut même proposer plusieurs versions.</p>
                        </div>
                    </div>

                </div>
            </div>
        </section>

    </div>

    <footer>
        <p>© 2026 Projet IA - Lycée 2S1</p>
        <p style="font-size: 0.8rem; opacity: 0.5;">Code optimisé للتجاوبية (Responsiveness)</p>
    </footer>

    <script>
        // SCRIPT POUR L'ANIMATION LORS DU SCROLL
        const observerOptions = {
            root: null,
            rootMargin: '0px',
            threshold: 0.1
        };

        const observer = new IntersectionObserver((entries, observer) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                    observer.unobserve(entry.target);
                }
            });
        }, observerOptions);

        document.querySelectorAll('section').forEach(section => {
            observer.observe(section);
        });
    </script>
</body>
</html>